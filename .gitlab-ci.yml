# This file is a template, and might need editing before it works on your project.
# Official framework image. Look for the different tagged releases at:
# https://hub.docker.com/r/library/node/tags/
image: avatsaev/angular-chrome-headless:latest

cache:
  paths:
  - node_modules/

before_script:
  - npm install

test_angular:
  script:
    - npm test

lint:
  script:
   - npm run lint

dist:
  script:
    - npm run dist

bundlesize:
  script:
    - npm run bundlesize

